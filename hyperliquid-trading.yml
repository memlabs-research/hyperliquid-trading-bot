name: hyperliquid-trading
channels:
  - conda-forge
  - defaults

dependencies:
  # Conda packages
  - python=3.10
  - numpy
  - pandas
  - pip
  - scikit-learn

  # Pip-only packages
  - pip:
      - requests
      - hyperliquid-python-sdk
      - websockets
      - eth_account
